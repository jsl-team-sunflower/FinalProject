<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunflower.mapper.AttachMapper">

	<insert id="insert">
		insert into hm_attach (uuid, uploadPath, uploadFile, filetype, productNum) 
		values (#{uuid}, #{uploadPath}, #{uploadFile}, #{filetype}, #{productNum})
	</insert>
	
	<select id="findByNum">
		select * from hm_attach where productNum = #{productNum}
	</select>
	
</mapper>